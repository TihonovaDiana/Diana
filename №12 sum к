#include <iostream> 
#include <cmath>
using namespace std;
int main()
{
	double sum = 0;
	int n;
	cout << "n=";
	cin >> n;
	int i = 1;
	double p = 0;
	for (   ; i <= n; i++)
	{
		p = p + sin(i);
		sum = sum + 1. / p;
	}
	cout << "sum (" << i << ") =" << sum << endl;
	system("pause");
	return 0;
}
